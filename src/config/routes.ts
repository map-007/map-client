export const HOME_URL = "/";

export enum AUTH_ROUTES {
  LOGIN = "/login",
  SIGNUP = "/signup",
}

export enum FOOTER_LINKS {
  HOME = HOME_URL,
  LEARN = "/learn",
  MERCHANT = "/merchant",
  SETTING = "/setting",
}
